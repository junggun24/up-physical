# Build / binaries
/bin/
*.exe
*.test
*.out

# Env / secrets
.env
deploy/.env
*.local.env

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# OS / editor
.DS_Store

# Harness working outputs (generated during verification runs)
.harness/tmp/
.harness/e2e/results/

# Android/Gradle (app/)
app/.gradle/
app/**/build/
app/local.properties
.kotlin/

# IDE (개인 환경 — 공유하지 않는다)
.idea/
*.iml

# 파이프라인 (pipeline/README.md 의 설치 절차로 재현)
.venv-pipeline/
pipeline/models/*.task

# 저작물 — 원본 영상은 리포에 넣지 않는다 (권리 근거는 DB 메타데이터로 관리)
*.mp4
*.mov
*.avi
