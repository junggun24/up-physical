# 로컬 개발 환경변수 — cp deploy/.env.example deploy/.env 후 사용.
# deploy/.env 는 gitignore 대상. prod 값은 절대 커밋하지 않는다.
DATABASE_URL=postgres://upx:upx@localhost:5433/upphysical
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
JWT_SECRET=dev-secret-change-me
ENGINE_SCRIPT=engine/dev_engine_server.py
PORT=8080
ALLOW_DEV_AUTH=true
